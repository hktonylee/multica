services:
  runtime:
    build:
      context: ..
      dockerfile: runtime/Dockerfile
    image: multica-runtime:local
    volumes:
      - ../var/.multica:/root/.multica
      - ../var/.codex:/root/.codex
    ports:
      - "19514:19514"
    command: ["-lc", "multica daemon start --foreground"]
